services:
  - type: web
    name: your-app-name
    env: node
    buildCommand: npm install && npm run build
    startCommand: node build/index.js
    envVars:
      - key: NODE_ENV
        value: production